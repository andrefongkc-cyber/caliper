# Copy to .env for local settings. .env is gitignored: never commit it.
#
# Nothing in V1 reads environment variables yet. Add entries here as
# features need them, each with a comment saying what reads it.

# AI layer (not implemented yet)
# ANTHROPIC_API_KEY=
